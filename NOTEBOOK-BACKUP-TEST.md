# ทดลองสำรอง–นำเข้าสมุด (รอบ 6C)

รอบนี้เพิ่มเฉพาะไฟล์สำรองลายมือใน Quiz Engine กลาง ไม่แก้ Firebase, AI Worker, Apps Script หรือ Pages workflow

## วิธีลองบน iPad

1. เปิด GitHub Pages ผ่าน Safari → แท็บ Test → บท 2 → Quiz เติมคำตอบ
2. เปิดสมุดทดของข้อ 1 และ 2 แล้วเขียนคนละแบบ
3. กด **สำรอง / นำเข้าสมุด** บนแถบด้านบน ตรวจว่ารายการมีเฉพาะข้อที่เขียน
4. กด **สำรองสมุดเป็นไฟล์** เก็บ `.hubnote` ใน Downloads/แอป Files และตรวจว่าไฟล์อยู่จริง
5. ปิดหน้าต่างสำรอง เพิ่มลายมืออีกหนึ่งเส้น แล้วเปิด **นำเข้าสมุดจากไฟล์** เลือกไฟล์ข้อ 4
6. ระบบแสดงรายการข้อที่จะนำเข้า หากมีลายมือเดิมต้องติ๊กยืนยันก่อน กด **ยืนยันนำเข้าสมุด** เพื่อนำกลับไปเป็นลายมือในไฟล์สำรอง
7. ปิดหน้าต่าง เปิดสมุด เขียนต่อ แล้วรีเฟรช ตรวจว่าลายมือยังอยู่

ไม่ต้องล้างข้อมูลเว็บหรือถอนการติดตั้งเพื่อทดสอบ และควรปิดแท็บอื่นของ Quiz ชุดเดียวกันก่อนนำเข้า

## ขอบเขตสำคัญ

- ไฟล์เก็บเส้นปากกา สี ความกว้าง แรงกด รหัสชุดและรหัสข้อ ไม่ใช่ PDF และนำกลับมาแก้ไขได้
- **ไม่รวมคำตอบ คะแนน ข้อความเหตุผล หรือผลรับรอง AI** การนำเข้าไม่เปลี่ยนคะแนน แต่ลายมือที่นำเข้าต้องตรวจวิธีคิดใหม่
- นำเข้าตรง Quiz ชุดเดิมเท่านั้น ใส่ให้ผู้ใช้ปัจจุบัน ไม่มีการนำเข้าบัญชีคนอื่นจากไฟล์
- ข้อที่ไม่อยู่ในไฟล์ไม่ถูกเปลี่ยน หลังสำรองไม่ลบสมุดเดิม
- ข้อมูลในเว็บอยู่ในเบราว์เซอร์/เครื่องนี้ ไม่ซิงก์อัตโนมัติไปเครื่องอื่นหรือ Google Drive การล้างข้อมูลเว็บ/พื้นที่ถูกคืนอาจทำให้งานในเว็บหาย จึงต้องเก็บไฟล์สำรองแยกไว้
- รุ่นทดสอบจำกัด 20 MB, 500 ข้อ, 10,000 เส้น และ 250,000 จุดปากกาต่อไฟล์ ยังไม่ใช่ระบบเก็บสมุดทุกวิชา/โควตาพื้นที่/แบ่งไฟล์อัตโนมัติ
- ใช้กับ Quiz ที่เปิดผ่าน `quiz-player.html` เท่านั้น ยังไม่รวมสมุดหน้า standalone หรือ simulation แบบเก่า

## สิ่งที่ตรวจแล้วก่อนเผยแพร่

- ชุดทดสอบอัตโนมัติครอบคลุมการตรวจรูปแบบไฟล์ ข้ามบัญชี/ผิดชุด ลายมือเสีย ขีดจำกัด การนำเข้าทั้งชุดแบบไม่สำเร็จบางส่วน และความขัดแย้งกับอีกแท็บ
- ทดสอบผ่านหน้าเว็บ: วาดเส้นจริง, ตรวจรายการ, นำเข้าไฟล์สองข้อ, ปุ่มยืนยันถูกปิดจนอนุญาตให้ทับ, รีเฟรชแล้วยังอยู่ และเขียนเพิ่มหลังนำเข้าได้
- การดาวน์โหลดบน Safari/iPad ต้องให้ผู้ใช้ตรวจการเก็บลง Files จริงอีกครั้ง: เบราว์เซอร์ในเครื่องมือทดสอบไม่ส่งผลยืนยันไฟล์ดาวน์โหลดกลับมา จึงไม่ถือว่าผ่านการดาวน์โหลดครบวงจรบน iPad

นักพัฒนารันทดสอบซ้ำด้วย `pnpm test:notebook`, `pnpm test:resume`, `pnpm test:number` และ `pnpm build` ไฟล์ `scripts/fixtures/notebook-numeric-demo.hubnote` ใช้ทดสอบนำเข้ารูปตัวอย่างเฉพาะชุด numeric-input-demo-v1 ไม่ใช่ข้อมูลนักเรียน
