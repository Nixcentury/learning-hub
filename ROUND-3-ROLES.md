# Learning Hub — Round 3 Account Roles

บันทึกวันที่ 5 กันยายน 2026

## ผลงานรอบนี้

- เพิ่ม `js/roles.js` เป็นระบบสิทธิ์กลางที่ทุกหน้า HTML เรียกใช้ร่วมกันได้
- บัญชีใหม่เป็นนักเรียนเสมอ และไม่มีปุ่มให้ผู้ใช้เลือกเป็นครูหรือแอดมินเอง
- ตรวจสิทธิ์ `student`, `teacher` และ `admin` จาก Firebase
- เพิ่มปุ่มสิทธิ์บัญชีในแถบบนของ Hub
- เพิ่มแบบฟอร์มขอสิทธิ์ครูภาษาไทยและอังกฤษ
- เพิ่ม `admin.html` เป็นหน้าหลังบ้านแยกไฟล์
- แอดมินสามารถดูคำขอ อนุมัติ ปฏิเสธ และถอนสิทธิ์ครู
- เพิ่ม `js/role-admin.js` แยกงานหลังบ้านออกจากหน้า Hub
- เพิ่ม Multi-page build เพื่อให้ `index.html` และ `admin.html` ถูกนำขึ้นเว็บพร้อมกัน

## หลักความปลอดภัย

- การซ่อนปุ่มในหน้าเว็บไม่ถือเป็นการรักษาความปลอดภัย
- `roles.js` อ่านสิทธิ์จาก Firebase และทำงานแบบ fail-closed: หากอ่านไม่ได้ให้ถือเป็นนักเรียน
- เฉพาะบัญชีใน `learningHub/admins/{uid}` เท่านั้นที่ควรเขียน `teacherApprovals`
- ไม่มี API ในหน้าเว็บสำหรับเพิ่มสิทธิ์แอดมินให้ตัวเอง
- ผู้ใช้ทั่วไปอ่านได้เฉพาะโปรไฟล์ คำขอ และสถานะอนุมัติของบัญชีตนเอง
- หน้าแอดมินแสดง UID เมื่อยังไม่มีสิทธิ์ เพื่อให้ผู้ดูแลใช้ตั้งแอดมินคนแรกจาก Firebase Console

## ไฟล์ Firebase Rules

กฎที่ต้องใช้และวิธีตั้งแอดมินคนแรกอยู่ใน `FIREBASE-RULES-ROUND3.md`

กฎดังกล่าวยังไม่ได้ถูกนำไปใช้จริง เพราะต้องใช้สิทธิ์ผู้ดูแล Firebase และต้อง Merge กับ Rules เดิมเพื่อไม่ให้ Quiz หรือ Simulation เสียหาย

## ผล QA ในเครื่อง

- Lint ผ่าน
- Production build ผ่านทั้ง `index.html` และ `admin.html`
- ยังไม่แก้ GitHub Pages workflow
- GitHub Pages เผยแพร่ `index.html` และ `admin.html` ผ่าน workflow เดิมแล้ว และทั้งสองหน้าเปิดได้ด้วยสถานะ 200
- ตรวจหน้า Quiz และ Simulation ตัวแทนหลังเผยแพร่แล้ว ยังเปิดได้ด้วยสถานะ 200
- ทดสอบ Google และการเขียน Firebase จริงต้องให้เจ้าของบัญชีดำเนินการหลังติดตั้ง Rules

## สิ่งที่ยังไม่ทำ

- ยังไม่เปิดใช้ Firebase Rules จริง
- ยังไม่กำหนดแอดมินคนแรก
- ยังไม่สร้าง Classroom Dashboard
- ยังไม่เชื่อม Role ไปหน้า HTML ลูก
- ยังไม่เปิดรายชื่อ Presence สำหรับครู
