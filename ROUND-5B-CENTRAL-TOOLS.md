# Learning Hub — Round 5B Central Content and Print Tools

บันทึกวันที่ 7 กันยายน 2026

## เป้าหมายรอบนี้

- แยกเนื้อหาออกจากระบบ Quiz อย่างเด็ดขาด
- ให้ไฟล์ HTML แต่ละชุดมีเฉพาะสรุป โจทย์ ตัวเลือก และข้อมูลคำตอบ
- ใช้หน้าตา ช่องตอบ และฟอร์มปริ้นชุดเดียวจากเครื่องมือกลาง
- ตรวจไฟล์ที่ AI สร้างก่อน Build และตรวจซ้ำตอนเปิดหน้า
- ยังไม่ตรวจคำตอบ คิดคะแนน บันทึกข้อมูล หรือสร้าง Quiz จริง

## โครงที่ได้

```text
public/content/**/*.html
        ↓ โหลดและตรวจ
public/shared/activity-core.js
        ↓ จัดหน้าจอและสร้างช่องตอบ
public/shared/print-core.js
        ↓
ฟอร์ม A4 มาตรฐานเดียวกันทุกชุด
```

ไฟล์ตัวอย่างอยู่ที่ `public/content/samples/activity-round-5b.html` ส่วนหน้า Shell สำหรับ QA อยู่ที่ `public/pages/tools/activity-preview.html`

หน้า Preview เป็นเครื่องมือหลังบ้าน/พัฒนาแบบลิงก์ตรง ไม่แสดงเป็น Quiz ในรายการของนักเรียน เมื่อมีเนื้อหาจริงจึงค่อยลงทะเบียน Content ID และแสดงการ์ดของเนื้อหานั้น

เลือกไฟล์ที่จะตรวจได้ด้วย `?content=<ที่อยู่ต่อจาก public/content>` เช่น `?content=physics/ohms-law.html` โดยระบบปฏิเสธพาธที่พยายามออกนอกโฟลเดอร์ Content

## Activity Content V1

- รองรับโจทย์ `choice` และ `number`
- ทุกชื่อเรื่อง โจทย์ และตัวเลือกต้องมี `data-th` กับ `data-en`
- แต่ละกิจกรรมและคำถามต้องมีรหัสภาษาอังกฤษตัวเล็กที่ไม่ซ้ำ
- โจทย์ตัวเลขต้องกำหนดคำตอบและค่าคลาดเคลื่อน
- ไฟล์เนื้อหาห้ามมี Script, CSS, Form, Button, Input หรือ Event handler
- Activity Core เป็นผู้สร้าง Radio และช่องตัวเลข จึงทำให้ทุกชุดมีหน้าตาเดียวกัน

คำตอบยังอยู่ฝั่ง HTML จึงเหมาะกับแบบฝึกหัดทั่วไป ไม่ใช่ข้อสอบที่ต้องปกปิด Answer Key

## Print Core

- ปุ่มปริ้นมาจาก Shell กลาง ไม่อยู่ในไฟล์เนื้อหา
- เลือกข้อที่จะพิมพ์เป็นรายข้อ เลือกทั้งหมด หรือล้างทั้งหมดได้
- มี 3 โหมด: พิมพ์สรุป, ปริ้นโจทย์เปล่า และปริ้นเฉลย
- ปริ้นเฉลยแสดงคำตอบที่ถูก และแสดง `data-question-solution` เมื่อผู้สร้างเนื้อหาใส่มา
- ใช้กระดาษ A4 แนวตั้งและระยะขอบเดียวกัน
- มีช่องชื่อ–นามสกุล ชั้น/ห้อง และเลขที่
- ซ่อนปุ่ม สถานะ Context และข้อความสำหรับ QA ตอนปริ้น
- ป้องกันการตัดโจทย์และตัวเลือกกลางข้อเท่าที่เบราว์เซอร์รองรับ
- ใช้ได้ทั้งข้อความไทยและอังกฤษ

## ตัวตรวจ Content

คำสั่ง `pnpm validate:content` ตรวจทุกไฟล์ใต้ `public/content` และถูกเรียกอัตโนมัติก่อน `pnpm build`

ทดสอบเชิงลบแล้วว่าไฟล์ที่มี `<script>` และโครงไม่ครบถูกปฏิเสธก่อน Build จากนั้นลบไฟล์ทดสอบออกแล้ว

## ผล QA

- Content Validator ผ่านกับไฟล์ตัวอย่างจริง 1 ไฟล์
- Lint ผ่าน
- Type check ผ่าน
- Production build ผ่าน
- โหลด HTML เนื้อหาแยกไฟล์บน localhost ผ่าน
- เลือกไฟล์ผ่าน `?content=` และปฏิเสธพาธนอก `public/content` ผ่าน
- สร้าง Radio 3 ตัวและช่องคำตอบตัวเลข 1 ช่องจาก Activity Core ผ่าน
- กรอกชื่อ ห้อง เลขที่ ตัวเลือก และตัวเลขผ่าน
- ภาษาไทยและอังกฤษเปลี่ยนครบทั้งเนื้อหา ช่องตอบ และปุ่มปริ้น
- หน้าจอปกติ, iPad 768px และมือถือ 390px ไม่มีการล้นแนวนอน
- ปุ่มปริ้นเรียก Print Core ได้โดยไม่มี Console error
- เบราว์เซอร์อ่านกฎ A4 และ `break-inside: avoid` สำหรับโจทย์ได้
- ทดสอบเปิดผ่าน Hub และรับ Context `physics-c1-activity / main / page-001` ผ่านก่อนนำการ์ด QA ออกจากฝั่งนักเรียน
- Workflow ของ GitHub Pages ไม่ถูกแก้ไข

## รอบถัดไป

Round 5C เพิ่ม Quiz Checker พื้นฐานสำหรับตัวเลือกและคำตอบตัวเลข แสดงผลรายข้อและคะแนนรวมในเครื่องเท่านั้น โดยยังไม่เชื่อม Firebase หรือ Classroom
