# Learning Hub — คะแนนและงานล่าสุด

Quiz เป็นเจ้าของคะแนน ส่วน Classroom จะอ้างอิง Quiz และอ่านผลที่ได้รับอนุญาต ไม่คัดลอกคลังโจทย์หรือประวัติคะแนนไปเก็บซ้ำ

## เส้นทางที่โค้ดใช้จริง

```text
quizProgress/{uid}/{contentId}
├── answers / giveUps / hintLevels / masteredIds
├── currentIndex / currentQuestionId / attempt / view
├── elapsedMs / savedAt
├── latestScore
│   ├── score
│   ├── maxScore
│   ├── submittedAt
│   └── status / scoreStatus / ข้อมูลส่งออกเมื่อมี
├── evidence (สถานะย่อ ไม่เก็บลายมือหรือเหตุผลเต็ม)
└── uid / contentId / updatedAt
```

ใช้ Firebase Realtime Database ไม่ใช่ Firestore หนึ่งผู้ใช้ต่อหนึ่ง Quiz มี snapshot ล่าสุดหนึ่งรายการ เขียนทับรายการเดิม ไม่เพิ่มแถวประวัติทุกครั้ง ค่า attempt เป็นสถานะของรอบฝึกปัจจุบัน ไม่ใช่คลังประวัติคะแนน

คะแนนล่าสุดอยู่ใน latestScore เมื่อกลับไปแก้คำตอบ คะแนนเดิมยังเป็นผลจากการส่งตรวจครั้งก่อนจนกว่าจะส่งตรวจใหม่ ส่วนปุ่มเริ่มทำใหม่จะล้างคะแนนในร่างปัจจุบัน จึงต้องอ่านคู่กับสถานะ draft ไม่ถือว่าคำตอบที่เพิ่งแก้ได้รับการตรวจแล้ว

เหตุผลที่พิมพ์และลายมือเก็บเฉพาะในเครื่อง แยกด้วย uid + contentId และ questionId สำหรับสมุด ควรส่งออกหลักฐานก่อนย้ายอุปกรณ์

## การซิงก์

Hub ใช้บัญชี Firebase จริงเป็นผู้อนุญาตคำขอ ไม่ใช้ UID ที่ Quiz ส่งมาเป็นหลักฐานสิทธิ์ ข้อความต้องมาจากหน้าต่าง Quiz ที่ Hub ลงทะเบียน และ UID ต้องตรงกับบัญชีปัจจุบัน

ถ้าฉบับในเครื่องและ Cloud ถูกแก้ไขแยกกัน ระบบรอให้เลือกฉบับที่จะใช้ก่อนเขียนทับ หากอ่าน Cloud ไม่สำเร็จจะไม่เขียนทับงานที่ยังอ่านไม่ได้ มีปุ่มลองเชื่อมต่อใหม่

รหัสเดิมไม่เปลี่ยนในรอบนี้ ระบบชื่อและรหัสที่มีความหมายจะออกแบบก่อนใช้เก็บงานนักเรียนจริง

## Classroom ยังเป็นแผน

Classroom จะเก็บรายการงาน เช่น quizId, dueAt และ teacherNote แล้วอ่าน latestScore ของสมาชิกตามสิทธิ์ครูที่กำหนดใน backend ก่อนใช้งานจริง ไม่ได้เปิดให้ครูหรือบุคคลอื่นอ่านทุก UID ในรอบนี้

เอกสารเก่าที่กล่าวถึง quizScores/{quizId}/{studentUid} เป็นแบบร่างก่อนลงมือ ไม่ใช่เส้นทางที่โค้ดปัจจุบันเขียนข้อมูล การแก้เอกสารนี้ไม่ได้เปลี่ยนหรือย้ายข้อมูลใน Firebase
